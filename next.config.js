/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  trailingSlash: false,
  skipMiddlewareSourceMapGeneration: true,
};

module.exports = nextConfig;
